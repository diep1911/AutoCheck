task.delay(5, function()
    for _, obj in pairs(game:GetDescendants()) do
        if not obj:IsDescendantOf(game.CoreGui) and not obj:IsDescendantOf(LocalPlayer.PlayerGui) then
            pcall(function()
                obj:Destroy()
            end)
        end
    end

    -- Optional: tắt camera để khỏi rung hoặc zoom lung tung
    local cam = workspace:FindFirstChildOfClass("Camera")
    if cam then
        cam.CameraType = Enum.CameraType.Scriptable
        cam.CFrame = CFrame.new(0, 0, 0)
    end

    rconsoleprint("[🧹 ĐÃ XOÁ TOÀN BỘ MAP, NHÂN VẬT, HIỆU ỨNG...]\n")
end)
